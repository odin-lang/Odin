// rexcode  ·  Brendan Punsky (dotbmp@github), original author

package rexcode_rsp_generated

// GENERATED by ../gen.odin -- DO NOT EDIT.
// Stage B: serialize the typed tables above to raw blobs under ../../tables/.

import "core:os"
import "core:fmt"

TABLES :: #directory + "/../../tables/"

raw :: #force_inline proc "contextless" (p: rawptr, n: int) -> []u8 {
	return (cast([^]u8)p)[:n]
}

w :: proc(file: string, data: []u8) {
	if err := os.write_entire_file(file, data); err != nil {
		fmt.eprintfln("rexcode tablegen: failed to write %s: %v", file, err)
		os.exit(1)
	}
}

main :: proc() {
	w(TABLES + "rsp.encode_forms.bin", raw(&ENCODE_FORMS, size_of(ENCODE_FORMS)))
	w(TABLES + "rsp.encode_runs.bin", raw(&ENCODE_RUNS, size_of(ENCODE_RUNS)))
	w(TABLES + "rsp.entries.bin", raw(&DECODE_ENTRIES, size_of(DECODE_ENTRIES)))
	w(TABLES + "rsp.idx_primary.bin", raw(&DECODE_INDEX_PRIMARY, size_of(DECODE_INDEX_PRIMARY)))
	w(TABLES + "rsp.idx_special.bin", raw(&DECODE_INDEX_SPECIAL, size_of(DECODE_INDEX_SPECIAL)))
	w(TABLES + "rsp.idx_regimm.bin", raw(&DECODE_INDEX_REGIMM, size_of(DECODE_INDEX_REGIMM)))
	w(TABLES + "rsp.idx_cop2.bin", raw(&DECODE_INDEX_COP2, size_of(DECODE_INDEX_COP2)))
	w(TABLES + "rsp.idx_lwc2.bin", raw(&DECODE_INDEX_LWC2, size_of(DECODE_INDEX_LWC2)))
	w(TABLES + "rsp.idx_swc2.bin", raw(&DECODE_INDEX_SWC2, size_of(DECODE_INDEX_SWC2)))
}
